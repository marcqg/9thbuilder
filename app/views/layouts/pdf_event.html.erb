<!DOCTYPE html>
<html lang="fr">
<head>
  <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
  <title>9thbuilder • <%= content_for?(:title) ? yield(:title) : "Bienvenue" %></title>
  <%= stylesheet_link_tag "pdf", :media => "all" %>
</head>
<body>

<div id="wrapper" class="container container-pdf">
  <div id="header">
    <h1>
      <strong>The 9th Age</strong>
      Army Builder
    </h1>
    <h2><%=transliterate @event.name.truncate(50) %></h2>
  </div>

  <div id="content">
    <%= yield %>
  </div>

  <div id="footer">
    <div class="user_name"><%=transliterate @event.name %></div>
    <div class="site"><%= link_to "9thbuilder.com" %></div>
    <div class="today"><%= @event.updated_at.strftime "%d/%m/%Y" %></div>
  </div>
</div>

</body>
</html>
