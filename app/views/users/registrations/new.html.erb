<% title t('signup.title') -%>

<h2><%=t 'signup.title' %></h2>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= devise_error_messages! %>

  <p><%= f.text_field :name, placeholder: t('signup.form.name'), autofocus: true %></p>

  <p><%= f.email_field :email, placeholder: t('signup.form.email') %></p>

  <p><%= f.password_field :password, placeholder: t('signup.form.password'), autocomplete: "off" %></p>

  <p><%= f.password_field :password_confirmation, placeholder: t('signup.form.confirmation'), autocomplete: "off" %></p>

  <p><%= f.collection_select :favorite_army_id, Army.all, :id, :name, include_blank: t('signup.form.favorite_army') %></p>

  <p><%= f.submit t('signup.form.submit') %></p>
<% end %>

<%= link_to "J'ai perdu mon mot de passe !", new_password_path(resource_name) %><br />
<%= link_to "Je n'ai pas reçu d'email de confirmation", new_confirmation_path(resource_name) %><br />
Pour toutes questions n'hésitez pas à consulter l'<a href="mailto:support@whbab.com">aide en ligne</a>
